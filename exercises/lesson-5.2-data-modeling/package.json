{
	"name": "taskflow-data-modeling-exercise",
	"version": "1.0.0",
	"private": true,
	"type": "commonjs",
	"scripts": {
		"build": "tsc",
		"typecheck": "tsc --noEmit",
		"anomalies": "npm run build && node dist/anomalies.js",
		"dashboard": "npm run build && node dist/dashboard.js",
		"counter": "npm run build && node dist/counter.js"
	},
	"dependencies": {
		"pg": "^8.13.1",
		"sequelize": "^6.37.5",
		"zod": "^3.24.1"
	},
	"devDependencies": {
		"@types/node": "^22.10.2",
		"typescript": "^6.0.3"
	}
}

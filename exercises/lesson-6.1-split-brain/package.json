{
	"name": "taskflow-split-brain-exercise",
	"version": "1.0.0",
	"private": true,
	"type": "commonjs",
	"scripts": {
		"build": "tsc",
		"typecheck": "tsc --noEmit",
		"detector": "npm run build && node dist/detector.js",
		"split-brain": "npm run build && node dist/scenario.js unfenced",
		"fenced": "npm run build && node dist/scenario.js fenced"
	},
	"dependencies": {
		"express": "^4.21.2",
		"zod": "^3.24.1"
	},
	"devDependencies": {
		"@types/express": "^4.17.21",
		"@types/node": "^22.10.2",
		"typescript": "^6.0.3"
	}
}

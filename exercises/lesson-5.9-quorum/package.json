{
	"name": "taskflow-quorum-exercise",
	"version": "1.0.0",
	"private": true,
	"type": "commonjs",
	"scripts": {
		"build": "tsc",
		"typecheck": "tsc --noEmit",
		"quorum": "npm run build && node dist/quorum.js",
		"partition": "npm run build && node dist/partition.js"
	},
	"devDependencies": {
		"@types/node": "^22.10.2",
		"typescript": "^6.0.3"
	}
}

# UI/UX Backlog

এই মুহূর্তে backlog খালি — সব issue, feature idea, আর lint debt ঠিক হয়ে গেছে। কী কী করা হয়েছে তার বিস্তারিত `BACKLOG_ARCHIVE.md`-এ আছে।

`bun run format`, `bun run lint`, `bun run check`, `bun run test:unit -- --run --project server`, আর `bun run build` — সবগুলো ক্লিন pass করে।
